# STDLIB.md

StoneKV uses only the Rust standard library.

The substitution table will be updated alongside implementation so that
only substitutions actually present in the final code are documented.